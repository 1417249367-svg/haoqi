///////////////////////////////////////////////////////////////////////////////////////////////////////////////
//短信运营商：彩云
//项目客户方：乐山
//代码:jc
//时间:2014-11-06 
///////////////////////////////////////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////////////////////////////////////
//生成发送短信URL(不同运营商，格式不一样)
///////////////////////////////////////////////////////////////////////////////////////////////////////////////
function get_sms_url(send_name,send_time,send_content,names,mobiles)
{
    var url = SEND_URL;
	var send_users = mobiles ;
	
    url = url.replace("{content}",send_content);
    url = url.replace("{users}",send_users);
	url = url.replace("{time}",send_time);
    return url ;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////
//返回短信结果(不同运营商，格式不一样)
//status(0失败 1成功)  msg(错误信息)
///////////////////////////////////////////////////////////////////////////////////////////////////////////////
function get_sms_result(response)
{
    var result = {"status":0,"msg":""};
    try
    {
		//{"MessageID":1816,"Msg":"信息发送成功！","ExecuteState":1}
        var sms_result = eval("(" + response + ")");
        result.status = sms_result.ExecuteState ;
        result.msg = sms_result.Msg;
		
    }
    catch(exception)
    {
        result.msg = data ;
    }
    return result ;
}

